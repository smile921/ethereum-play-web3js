
import { contract } from 'ethereum-play-web3js/helpers/contract';
import { module, test } from 'qunit';

module('Unit | Helper | contract');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = contract([42]);
  assert.ok(result);
});

